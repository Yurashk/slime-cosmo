# Slime Galaxy Merge — Sound Design Concept

Жанровый вектор: **"sonic candy"** — короткие, сочные, желеобразные звуки в неоновом космосе.
Главное правило: ни один звук не должен утомлять, даже если игрок делает 30 слияний в минуту.

---

## 1. Принципы (анти-утомление)

| Принцип | Реализация |
| --- | --- |
| Короткость | Все игровые звуки короче 400 мс (кроме unlock ~600 мс и game over ~700 мс) |
| Слои, а не объём | Каждое событие = 2–3 тонких слоя; частые звуки тише редких |
| Вариативность | Pitch-рандомизация ±8% на каждый звук + авто-транспозиция при повторе |
| Никаких идеальных повторов | Один и тот же звук дважды подряд → транспонируется вверх на +1 полутон |
| Ducking | Фоновый дрон автоматически уходит на −3 дБ на момент действия |
| Пауза вне фокуса | `document.hidden` → полный mute (и `AudioContext.suspend()`) |
| Лимит голосов | Максимум 6 одновременных звуков; старые — плавно сворачиваются |
| Короткий хвост | Лёгкий реверб (~400 мс) = «космос», но без эха после каждого клика |
| Умная громкость | Merge level 1–5 громче (частые события — но они должны радовать), комбо-каскады приглушаются, чтобы не было какофонии |

---

## 2. Карта звуков

### 2.1. Слияние слизней — главный звук
Самая частая и самая «сочно-заряженная» единица. Состоит из 3 слоёв:

1. **Bubble pop (лопающийся пузырь)** — `sine` + короткий импульс.
   - Быстрый downward-pitch: 180 → 70 Гц за 60 мс, громкость затухает за ~120 мс.
   - Даёт «плюх», физическое ощущение схлопывания.
2. **Taffy stretch (тянучка / резина)** — полосатый шум через bandpass.
   - Bandpass 400 → 900 Гц, 140 мс, `Q ~2`.
   - Даёт «тягучесть», желеобразность, ощущение что слизней растянуло перед схлопыванием.
3. **Sparkle chord (лёгкий аккорд)** — синтезированные pluck-ноты 2000–4000 Гц.
   - Очень тихий (≈ −18 dB относительно слоя 1), затухание 300 мс.
   - Даёт «магию» и reward-мгновение.

**Питч-лестница по уровню:**
```
basePitch(level) = 150 Hz * 1.08^(level-1)
```
Чем выше уровень, тем звук выше и «ярче» (добавляется громкость слоя 3). Прогрессия незаметная, но её слышно — коллекция ведёт игрока вверх.

**Планеты (уровни 6–14):** звук меняется по типу планеты:
- Газовые гиганты (Jupiter/Saturn): добавляется низкий «орбитальный» sub-bass 60–90 Гц.
- Кольчатые (Saturn/Uranus): добавляется звенящий metallic-хвост (синус 4–6 кГц с затуханием 500 мс).
- Солнце (level 14): самый полный звук — все 3 слоя + широкий «залповый» shimmer.

### 2.2. Респаун нового слизня («готовность»)
Тихий, не должен воровать внимание у слияний.
- **Puff**: 50 мс белого шума через low-pass 800 Гц, очень тихий (−20 dB).
- **Neon hum**: синус 220 → 330 Гц за 120 мс с быстрым decay — «зарядка энергии».
- Альтернация: два звука респауна подряд автоматически слегка меняют pitch (±2 полутона) — нет ощущения «пианино».

Если следующий слизень — планета: добавляется «звёздный вздох» (одна звенящая нота 3–5 кГц, 250 мс). Это подсказка/анонс, не спойлер-загрузка.

### 2.3. Комбо — эскалация «звёздной пыли»
Звуковая лестница, которая нарастает с каждым шагом внутри серии:

| Комбо | Что происходит |
| --- | --- |
| 2–3 | Базовый merge-звук +2 полутона, чуть громче + **shimmer** (белый шум через bandpass 6–9 кГц, 60–100 мс) — «звёздная пыль» |
| 4–6 | Добавляется **восходящее арпеджио** из 3–4 pluck-нот (пентатоника в тональности звука уровня), реверб шире |
| 7–9 | **Аккорд-взрыв**: базовый звук + восходящий аккорд из 4 нот + двойной shimmer-хвост, громкость на +3 dB сверх лимита с мягким компрессором |
| 10× (кап) | Короткая **фанфара-sparkle** — 5 нот восходящей глиссандо за 350 мс + «дождь» высокочастотных частиц |

Правило «вопрос → волна»: на 2–3 комбо звук как бы спрашивает «ещё?», дальше отвечает всё ярче. При разрыве серии (окно 1.5 с) всё сбрасывается к базовому звуку — пауза между «волнами» снимает утомление.

### 2.4. Unlock новой планеты
Самая редкая и самая запоминающаяся:
- Восходящая лестница из 3–5 «колокольных» тонов (синусы 500 → 2500 Гц, гармоник 2–4, decay 400 мс).
- Подложка — глубокий теплый гул 90 Гц с медленным fade-in/out 600 мс.
- Никаких резких атак: ощущение «нового мира открылся».

### 2.5. Drop слизня
Подтверждение действия, а не событие:
- **Plip**: 25 мс, «мокрый» короткий насыщенный щелчок с очень маленькой громкостью (−16 dB).
- Не путается со слиянием: drop — это точка тише, merge — точка громче.

### 2.6. Game Over
Мягкий, без агрессии:
- Нисходящий детонар 400 → 60 Гц за 500 мс.
- Глубокий «вздох» (sub-sine с ревербом), 700 мс.
- Намеренно тихий — экран статистики говорит сам за себя. Никакого «badum-tss».

### 2.7. UI / системные
- Кнопки: нейтральный микро-клик 10–15 мс.
- Ховер и драг-над canvas: БЕЗ звука (самое утомляющее — звук на каждое движение).
- Открытие лидерборда: шелест «шороха» (noise burst 80 мс, bandpass 3 кГц) — противоположность silence-фокусу игрового поля.

---

## 3. Техническая реализация (Web Audio API, без файлов)

- `AudioContext` создаётся лениво при первом пользовательском действии (autoplay policy).
- Звуки — чистый синтез: `OscillatorNode` + `GainNode` + `BiquadFilterNode` + `BufferSourceNode` (предзаполненный noise-буфер 0.6 с).
- Мастер-цепочка: `masterGain` → мягкий `DynamicsCompressorNode` → (опционально) короткий `ConvolverNode` с reverb-IR ~400 мс.
- Питч-лестница: `basePitch = 150 * Math.pow(1.08, level)`.
- Хэлперы (класс `SlimeAudio`):
  - `playDrop()`
  - `playMerge(level)`
  - `playRespawn(nextIsPlanet)`
  - `playCombo(streak)` — внутренняя лестница
  - `playUnlock()`
  - `playGameOver()`
  - `duck(t)` / `unduck(t)`
- Haptics: `navigator.vibrate(10–30 мс)` на merge и комбо (mobile) — вибро усиливает «сочно» без единого Hz звука.
- Сохранение настроек в `localStorage`; toggle `SFX ON/OFF` — продумать заранее, чтобы не переделывать.

## 4. Чек-лист «Сочно или нет»

- [ ] Слияние вызывает улыбку даже на 40-й минуте (не раздражение).
- [ ] Комбо слышно нарастает, но не превращается в кашу.
- [ ] Респаун почти не замечается, но без него игра «немая».
- [ ] Нет ни одного звука длиннее 400 мс в игровом цикле.
- [ ] Мьютится при свёрнутой вкладке.
- [ ] Пианино-эффект (одинаковые повторы) исключён вариативностью.